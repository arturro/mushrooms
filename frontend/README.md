# Frontend

This directory is reserved for the frontend application.

Status: placeholder (no code yet).

Suggested next steps:

- stack Vite + React/TS
- Add project manifest (e.g., package.json) and a minimal app skeleton.
- Align Node version and tooling with the backend as needed.
